{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "test-packages/*"
    ]
  },
  "scripts": {
    "start": "yarn workspace ember-fui-modules start",
    "test": "yarn workspaces foreach -pv run test --test-port=0"
  },
  "engines": {
    "node": "12.*"
  },
  "resolutions": {
    "@babel/preset-env": "7.10.2"
  }
}
